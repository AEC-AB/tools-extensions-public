# Dependencies

Direct dependencies per manifest, as declared in the source. The agent works offline and cannot check advisory databases; versions flagged "check advisories" need a human or a tool such as `dotnet list package --vulnerable`.
